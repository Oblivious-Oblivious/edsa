#ifndef __EDSA_LINKED_LIST_DUP_H_
#define __EDSA_LINKED_LIST_DUP_H_

#include "linked-list-base.h"

/**
 * @brief: Creates a duplicate linked list
 * @param list -> The linked list to duplicate
 * @return The duplicate
 **/
EdsaLinkedList *linked_list_dup(EdsaLinkedList *list);

#endif
